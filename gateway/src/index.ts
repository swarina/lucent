// Lucent collector + gateway. Real server (gRPC ingest, REST, WebSocket, static
// hosting) lands with M0-T9. This scaffold only proves the TS toolchain builds.

export const VERSION = "0.0.1";

function main(): void {
  // eslint-disable-next-line no-console
  console.log(`lucent-gateway ${VERSION} — scaffold (M0-T9 pending)`);
}

main();
