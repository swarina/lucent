{
  "name": "@lucent/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Lucent frontend: the live 'watch it think' interface.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.0",
    "vite": "^5.3.0",
    "vitest": "^2.0.0"
  },
  "$comment": "State (zustand), 3D (react-three-fiber, drei) and d3 land with M0-T10 / M1-T8."
}
